# Production Environment Variables
# When deployed, frontend is served by the backend on the same origin
# So API_URL is empty (relative paths like /api/auth/login will work automatically)
VITE_API_URL=
